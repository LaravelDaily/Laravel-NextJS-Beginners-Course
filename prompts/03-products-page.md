Create a new `/products` page where all the paginaged products will be shown. Add the menu item on top.

Fetch products from `GET /api/products`.

Display as a grid of `<ProductCard>` components.

Show category filter buttons at the top using links. For now, hardcoded categories:
- Strollers
- Car Seats
- Cribs
- High Chairs

Filter functionality will be handled later, for now I need the front-end page.