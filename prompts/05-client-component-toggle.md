Add a small Client Component example to this Next.js demo by implementing a view-mode toggle on /products that switches between the current grid layout and a compact list layout, with the selection updating instantly in the browser and ideally persisting in localStorage. Keep the page and data fetching as Server Components, create only a minimal 'use client' boundary for the interactive UI.